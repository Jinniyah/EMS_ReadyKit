# EMS ReadyKit — Python dependencies
# Last security audit: 2026-06-19
# Updated to resolve all pip-audit findings (SEC-1 / OWASP A06).

# Web framework
# fastapi 0.136.1 requires pydantic>=2.9.0 and starlette>=1.0.0
# starlette 1.3.1 clears CVE-2026-54283, CVE-2026-54282
fastapi==0.136.1
starlette==1.3.1
uvicorn[standard]==0.29.0
python-multipart==0.0.31  # 0.0.31 clears CVE-2026-53540, CVE-2026-53539, CVE-2026-53538
slowapi==0.1.9            # Rate limiting (OWASP A04 — wraps limits library, integrates with FastAPI)

# Database ORM + migrations
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9   # PostgreSQL driver (binary wheel — no libpq build dep)

# Pydantic v2
# 2.13.4 is the current stable v2 release; no breaking changes from 2.7.x for our usage
pydantic==2.13.4
pydantic-settings==2.14.2  # 2.14.2 clears GHSA-4xgf-cpjx-pc3j (pip-audit, CI run 2026-06-19)

# Azure SDK — Key Vault secret retrieval via Managed Identity
azure-keyvault-secrets==4.8.0
azure-identity==1.19.0   # was 1.16.0 — fixes CVE-2024-35255

# Logging helpers
python-json-logger==2.0.7

# Date/time utilities
python-dateutil==2.9.0

# Authentication — Azure AD JWT validation
# PyJWT 2.13.0 — fixes PYSEC-2026-175/176/177/178/179 (2026-06-02)
# cryptography 48.0.1 clears GHSA-537c-gmf6-5ccf
PyJWT[crypto]==2.13.0
cryptography==48.0.1
# httpx2 is the Pydantic-maintained continuation of httpx (same API, new package name).
# Versioned as 2.x (not 0.x). Replaces plain httpx for starlette 1.3.1 TestClient
# compatibility — without it, starlette fires StarletteDeprecationWarning on import.
httpx2==2.4.0

# Testing
# pytest 9.0.3 — fixes CVE-2025-71176
# pytest-asyncio removed: all tests are synchronous (TestClient-based)
pytest==9.0.3
pytest-cov==6.1.0
